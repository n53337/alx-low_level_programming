#include "main.h"

/**
 *_abs-prints the absolute value of an integer.
 *@a:calculate absolute value.
 *Return:abs
 *
 */
int _abs(int abs)
{
	int num = abs;

	if (num < 0)
	{
		num = num * (-1);
	}
	int absolute = num;
	return (absolute);
}
